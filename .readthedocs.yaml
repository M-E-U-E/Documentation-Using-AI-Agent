version: 2

build:
  tools:
    python: "3.11"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - method: pip
      requirements: requirements.txt
build:
  commands:
    - pip list
    - mkdocs build